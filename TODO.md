# TODO — дорожная карта acuity-yolo

Открытые задачи и идеи. Статусы: `[ ]` — не начато, `[~]` — в работе,
`[x]` — готово. Архитектура по каждой фиче обсуждается (см. «Открытые
вопросы» в конце) — фиксирую здесь как идеи, решения пока не приняты.

> Где считать логику — открытый вопрос. Сейчас воркер **изолирован по ТЗ**:
> только детекция (`POST /detect` → боксы), вся семантика — в inference-service
> (`acuity-server`). Часть фич ниже могут остаться там; некоторые (напр.
> общий описатель кадра) могут потребовать новых эндпоинтов здесь. По каждой —
> отдельное решение.

## Фичи

### Общее описание кадра каждую минуту
- [ ] Периодическая (раз в минуту) генерация текстового описания сцены на кадре
      («3 человека у входа, 1 машина на стоянке, дождь»).
- [ ] Источник: суммаризация детекций YOLO по времени (не отдельная модель
      на старте — но, возможно, VLM/Captioning-модель тиража 2).

### Детекция очередей
- [ ] Группировка боксов `person` в кластеры (плотность + время) → «очередь».
- [ ] Параметры: мин. число людей, радиус кластера, время удержания.
- [ ] Метрика: длина очереди (чел.), среднее время ожидания.

### Детекция загруженности стоянки
- [ ] Подсчёт занятых/свободных мест по боксам `car`/`truck`/`bus`/`motorcycle`.
- [ ] Опционально: матчинг с размеченными слотами парковки (polygon per slot).
- [ ] Метрика: N занято / M всего, % заполнения.

### Автоматическое открывание ворот
- [ ] Триггер: детекция `person`/`car` в заданной зоне (ROI) у ворот.
- [ ] Правила антидребезга: подтверждение по N кадрам / времени удержания.
- [ ] Интеграция: webhook/команда в систему ворот (outbound — ломает
      изоляцию воркера; вынести в inference-service?).

### Забытые вещи
- [ ] Детекция стационарных предметов (`suitcase`, `handbag`, `backpack`,
      `baggage`) дольше порога времени без движущегося `person` рядом.
- [ ] Требует трекинга между кадрами (пока воркер stateless — нужен стейт
      в inference-service или отдельный трекер).

### Изменение погоды
- [ ] Классификация по кадру: ясно / облачно / дождь / снег / туман / ночь.
- [ ] Варианты: (а) отдельная small-модель классификации, (б) эвристики по
      гистограмме/яркости, (в) внешнее API погоды по координатам камеры.

## Классификация событий

- [ ] Единый слой правил поверх детекций: входы — классы/счётчики/зоны/время,
      выход — тег события (`queue`, `parking_full`, `gate_open`, `abandoned`,
      `weather_rain` и т.д.).
- [ ] Сводная таблица: событие → источник(и) → пороги → действие.
- [ ] Тегирование событий в БД (inference-service).

## Технический долг / инфра

- [ ] `names.json` рядом с `.onnx` для своих наборов классов (помечено
      «тираж 2» в `app/model.py`).
- [ ] `UndefinedVar: $LD_LIBRARY_PATH` в `Dockerfile` (builder-стадия) —
      lint Docker ругается, работает за счёт пустого значения.
- [ ] `Node.js 20 is deprecated` в CI — апгрейд actions до версий на Node 24.
- [ ] Бенчмарк-скрипт: прогон всех размеров YOLO (n/s/m/l/x) на своих
      картинках для обоснованного выбора модели.
